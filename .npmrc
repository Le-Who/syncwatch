# Tell pnpm to use a flat node_modules folder (like npm/yarn) instead of symlinks.
# This prevents Next.js Turbopack from crashing with "We couldn't find the Next.js package"
# due to buildpack cache layers restricting symlink resolution outside the workspace.
node-linker=hoisted
